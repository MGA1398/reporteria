import { DiseaseFilterPipe } from './disease-filter.pipe';

describe('DiseaseFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DiseaseFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
